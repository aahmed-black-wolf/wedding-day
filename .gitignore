/node_modules
/.next
/out
/build
.DS_Store

# Anime conversion tooling — dev only, not committed (output WebP frames in public/ ARE committed)
/tools/anime/work
/tools/anime/.venv
__pycache__/

# Guest RSVP data (server-side store) — keep private, not in git
/data

.env.local
.env*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
